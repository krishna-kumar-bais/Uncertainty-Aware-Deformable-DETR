# Python 3.8-compatible runtime requirements for this project.
#
# This server is running Python 3.8.10, so keep these versions pinned exactly.
# We intentionally omit torchaudio and gdown because this project does not use
# them at runtime and removing them reduces installation risk on the GPU server.
#
# PyTorch wheels should match the server CUDA compiler (`nvcc --version`).
# This server reports CUDA 11.8, so use the cu118 wheels.
#
# Then install the remaining dependencies from this file:
#   pip install -r Deformable-DETR/requirements.txt

torch==2.4.1+cu118
torchvision==0.19.1+cu118
numpy==1.24.4
scipy==1.10.1
cython==3.0.10
pycocotools==2.0.7
tqdm==4.66.5
matplotlib==3.7.5
Pillow==10.4.0
packaging==24.1
ninja==1.11.1.1
